---
- name: Homepage neu starten
  ansible.builtin.systemd_service:
    name: homepage
    scope: user
    state: restarted
  # Beim allerersten Lauf gibt es die Unit noch nicht - dann ist das kein Fehler.
  failed_when: false
